# Changelog

## 1.1.0
- API курсов: `GET /api/currency` — отдаётся то, что уже лежит в кэше сайта, к ЦБ ходит сам сайт раз в час
- Получение курсов вынесено в `getCurrencyCourses()`, вывод в сайдбаре использует её же
- Импорты переведены с `App\Support` на `App\Support`
- Требует ядро 14.3.0

## 1.0.1
- Переход на datetime, минимальная версия движка 14.1.0

## 1.0.0
- Первый релиз
