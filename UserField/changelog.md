# Changelog

## 1.0.3
- Удаление поля в админке переведено на ядровой `data-ajax`, свой JS-обработчик больше не нужен

## 1.0.2
- Добавлены тесты (`Tests/Feature/UserFieldTest.php`): сохранение и валидация полей профиля, санитизация textarea, чистка при удалении пользователя
- Импорты переведены с `App\Classes` на `App\Support`, минимальная версия ядра — 14.3.0

## 1.0.1
- Вьюхи переведены с устаревшего `getInput()` на стандартный `old()`

## 1.0.0

- Пользовательские поля профиля вынесены из ядра в модуль
